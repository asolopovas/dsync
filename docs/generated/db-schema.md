# Generated DB schema

Dsync does not own an application database schema. It streams user-provided MySQL/MariaDB dumps between environments.

If future tests generate a stable fixture schema, store the generated summary here and include the generation command.
